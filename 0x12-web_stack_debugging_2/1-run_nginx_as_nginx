#!/usr/bin/env bash
# Run nginx under nginx user

pkill apache2
sed -i "s/80 default_server/8080 default_server/" /etc/nginx/sites-ena/default
chmod +r /etc/nginx/nginx.conf
sudo -u nginx service nginx restart
